<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Animación de Circuito</title>
    
    <style>
        body, html {
    margin: 0;
    padding: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
    background-color: #000; /* Fondo negro para simular un fondo de circuito */
}

.circuit-background {
    width: 100%;
    height: 100%;
    overflow: hidden;
    animation: moveCircuit 10s linear infinite;
}

@keyframes moveCircuit {
    from {
        transform: translateX(0);
    }
    to {
        transform: translateX(-100%);
    }
}

    </style>
</head>
<body>
    <div class="circuit-background">
        <svg xmlns="http://www.w3.org/2000/svg" version="1.1" width="100%" height="100%">
            <!-- Definir patrones para el fondo -->
            <defs>
                <pattern id="circuit-pattern" x="0" y="0" width="100" height="100" patternUnits="userSpaceOnUse">
                    <!-- Líneas curvas -->
                    <path d="M0 50 Q 25 0 50 50 T 100 50" stroke="rgba(0, 128, 255, 0.8)" stroke-width="2" fill="none" />
                    <path d="M50 0 Q 100 25 50 50 T 50 100" stroke="rgba(0, 128, 255, 0.5)" stroke-width="2" fill="none" />
                </pattern>
            </defs>
            <!-- Usar el patrón como fondo -->
            <rect width="100%" height="100%" fill="url(#circuit-pattern)" />
        </svg>
    </div>
</body>
</html>
